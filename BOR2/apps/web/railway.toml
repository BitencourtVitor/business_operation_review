[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
watchPatterns = ["BOR2/apps/web/**"]

[deploy]
startCommand = "bun server.js"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
