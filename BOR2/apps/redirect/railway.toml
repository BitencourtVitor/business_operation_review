[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
watchPatterns = ["BOR2/apps/redirect/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
